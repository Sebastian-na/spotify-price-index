# Spotify Price Index (Technical test)
## Installation
Clone the repository
`$ git clone https://github.com/Sebastian-na/spotify-price-index.git `
Install dependencies
`$ npm install`
And run:
`$ npm run dev`
